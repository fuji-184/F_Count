[package]
name = "f_count"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[features]
default = []
hardware = ["dep:perf-event"]
software = ["dep:perf-event"]
time = []
rdtscp = []

[dependencies]
perf-event = { version = "0.4.9", optional = true }
